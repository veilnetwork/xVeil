name: mobile_scanner
description: xVeil simulator-only camera stub; never used in production builds.
version: 5.2.3
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
